# Where the FastAPI backend is listening.
# Defaults to http://127.0.0.1:8000 (what run_api.bat serves) when unset, so
# this file is only needed if you move the API.
#
# Copy to .env.local to use it. Vite only exposes variables prefixed VITE_.
VITE_API_BASE_URL=http://127.0.0.1:8000
